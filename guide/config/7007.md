# 回调地址

回调用于将投递成功、失败、退信等异步事件通知业务系统。

- 使用可从公网访问的 HTTPS 地址。
- 接口应快速返回成功响应，耗时处理放入异步队列。
- 根据文档提供的机制验证回调来源或签名。
- 按事件唯一标识实现幂等，避免重复回调造成重复处理。
- 日志中隐藏收件地址、令牌和业务敏感字段。

配置后应使用测试任务验证连通性。不要把“已提交发送”当作“已投递成功”，最终状态应以投递事件为准。
