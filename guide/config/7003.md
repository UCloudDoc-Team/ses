# 发信地址

发信地址由本地部分和已验证域名组成，例如 `notice@example.com`。

- 使用能表达用途的本地部分，如 `notice`、`security` 或 `billing`。
- 显示名称与品牌或业务主体保持一致。
- 事务通知和营销邮件使用不同地址，便于管理信誉和退订。
- 如需接收用户回复，请确保回复地址真实可用。

停用发信地址前，应确认没有正在运行的任务或应用仍引用该地址。
