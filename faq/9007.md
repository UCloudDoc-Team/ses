# 投递与回调

## 对方服务器已接收，为什么用户仍没看到邮件？

邮件可能进入垃圾箱、推广分类或被收件人规则移动。请让用户搜索发件人和主题，并检查垃圾箱。

## 为什么没有收到回调？

检查回调地址是否公网可达、HTTPS 证书是否有效、接口是否及时返回成功响应，以及防火墙是否拦截。

## 回调为什么会重复？

网络重试可能造成同一事件多次送达。业务系统应以事件或消息唯一标识实现幂等处理。
