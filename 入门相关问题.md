### 如何用一些简单的方法测试邮件发送呢？

客户可以使用自己的账号进行测试，我们有100封的免费额度，目前不提供测试账号。

### 是否可以一开始就立刻发送大量邮件呢？

因为收件电子邮件服务商（ISP）限制的原因，如果是没有进行过批量发信的域名或 IP，不建议在一开始就立刻发送大量邮件，新域名/IP开始发信之前都要进行 Warm Up，以获得良好的邮件发送效果。

### 什么是 Warm Up？

Warm Up 是指发信域名、发信 IP 的预热过程。电子邮件的投递过程较为复杂，不同的电子邮件服务提供商（ISP）一般对每个发信域名/IP 的日发送总量有限制，如果是没有进行过批量发信的域名/IP，不可以在一开始就单天发送大量邮件，为了可以取得良好的邮件送达效果，发送量必须是每日递增式的，最终达到单日大量发送的效果。每日递增的发信过程即预热，也叫 Warm Up。预热过程对于同一个域名/IP一般只需要进行一次即可。

### 如何获取和配置发信域名？

发信域名是用于发送邮件的域名，需要在控制台完成域名配置与验证，通过添加相应的 DNS 解析记录来验证域名归属。验证通过后，即可使用该域名进行发信。

### 什么是 SPF、DKIM、DMARC？

SPF、DKIM、DMARC 是三种常用的邮件身份验证机制：

- SPF：通过 DNS 记录声明哪些服务器有权以该域名发信；
- DKIM：对邮件内容进行数字签名，收件方可验证邮件未被篡改；
- DMARC：结合 SPF 和 DKIM 的验证结果，告知收件方如何处理未通过验证的邮件。

正确配置这三项能有效提升邮件的可信度与送达率。

### 什么是退信？硬退信和软退信有什么区别？

退信是指邮件无法成功投递、被收件方退回的情况。

- 硬退信（Hard Bounce）：由于收件地址不存在、域名无效等永久性原因导致的退信，这类地址不应再重试；
- 软退信（Soft Bounce）：由于收件方邮箱已满、临时网络问题等暂时性原因导致的退信，通常可稍后重试。


